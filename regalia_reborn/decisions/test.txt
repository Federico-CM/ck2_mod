decisions = {
	
	sacred_withdrawal = {
		ai = no
		only_playable = yes

		potential = {
			
		}
		allow = {
			prestige = 25
			scaled_wealth = 0.25
			prisoner = no
			religion_group = estelley
			NOT = {
				has_character_modifier = in_seclusion
			}
		}
		effect = {
			prestige = -25
			scaled_wealth = -0.25
			# spawn_good_commander_effect = yes

			custom_tooltip = { text = estelley1 } # the description at the tooltip
			character_event = { id = RR.1 }
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 0
		}
	}
}
